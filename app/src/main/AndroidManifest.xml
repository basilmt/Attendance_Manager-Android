<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.revolt.attendancemanager">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AttendanceManager">
        <activity android:name=".DemoActivity"></activity>
        <activity android:name=".ShowAttendanceActivity" />
        <activity
            android:name=".AttendanceUserListerActivity"
            android:theme="@style/Theme.AttendanceManager.NoActionBar" />
        <activity android:name=".RegNewUserActivity" />
        <activity
            android:name=".NewUserListerActivity"
            android:theme="@style/Theme.AttendanceManager.NoActionBar" />
        <activity android:name=".ShowUserActivity" />
        <activity
            android:name=".UserListerActivity"
            android:theme="@style/Theme.AttendanceManager.NoActionBar" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>